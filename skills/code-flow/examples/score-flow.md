# score-flow

> Generic markdown render of [`score-flow.json`](./score-flow.json) — the
> code-walk of a score (inference) request through MWC and the ACA
> inference container. Base mode: no per-step `status`, no incident block.
>
> Mode: `base` · 17 steps · schemaVersion 1.0.0
>
> Re-render hint: this file is what the `code-flow` skill produces from a
> base-mode `flow.json` without any consumer-specific enrichment (no Kusto
> queries, no docsify directives, no ADO line-jump URLs). Downstream
> skills (knowledge-base, code-insight, …) take the same JSON and produce
> richer renders.

| # | Kind | Title | Entity (repo / class / function / line) | Action |
| --- | --- | --- | --- | --- |
| 1  | call-step      | Caller sends score POST                                | external · `<caller>`                                                                                                          | Caller sends POST `.../mlmodels/<artifactId>/endpoint/versions/<version>/score` with the JSON / DataFrame payload. The `x-ms-parent-activity-id` header is the correlation key into backend traces. |
| 2  | call-step      | MWC score controller receives request                  | workload-ml · `ModelEndpointMwcController.InvokeScoreModelEndpointVersionAsync` · L221                                          | Confirms the request reached the MWC API and routes it to `ScoreVersionAsync` on the manager. *(logAnchor: `Checking for moniker eviction for moniker Id`)* |
| 3  | failure-point  | Read endpoint metadata (Redis → Cosmos fallback)       | workload-ml · `ModelEndpointManagerACA` · L728                                                                                  | Reads endpoint metadata: tries the Redis cache first, falls back to Cosmos central store. Throws `EndpointNotFound` if neither has a record. |
| 4  | failure-point  | Resolve requested version                              | workload-ml · `ModelEndpointManagerACA` · L729                                                                                  | Resolves the request `version` string against known versions. If empty, short-circuits with `404 NotFound`. |
| 5  | failure-point  | Version-state precondition                             | workload-ml · `ModelEndpointManagerACA` · L1066                                                                                 | Asserts `versionState in (Running, Updating, UpdateFailed)` AND `envState == Running`. If false, returns `409 Conflict` (V2) or Initializing branch (V1). |
| 6  | call-step      | Scale-down probe (Redis)                               | workload-ml · `ModelEndpointManagerACA.IsModelEndpointVersionScaledDown` · L1070                                                | Queries Redis for the version's ACA replica count to decide whether to wrap the forward call in a cold-start cancellation timeout. |
| 7  | failure-point  | Forward score request to ACA container                 | workload-ml · `ModelEndpointManagerACA.ForwardScoreRequestAsync` · L1091                                                        | Builds the ACA managed-environment URL, fetches an MSI token, POSTs streamed payload to the inference container. Throws `TaskCanceledException` on cold-start timeout or HTTP error. *(logAnchor: `Executed Container app API: response code`)* |
| 8  | failure-point  | NGINX ingress accepts POST                             | FabricDS-ModelEndpoint · `docker/endpoint-ingress/nginx.conf.top` · L34                                                         | Request lands on endpoint-ingress NGINX port 8080. If no per-version location block exists, default returns `503 "Version Unavailable"`. Upstream failures map to `502`/`504`. *(logAnchor: `POST /versions/`)* |
| 9  | failure-point  | JWT auth subrequest                                    | FabricDS-ModelEndpoint · `auth_endpoint.authorize` · L163                                                                       | NGINX `auth_request /auth` issues an internal subrequest to Gunicorn sidecar; bearer JWT decoded with RS256 against tenant JWKS; `oid` checked against `ML_WORKLOAD_PRINCIPAL_ID` allowlist. Returns `401`/`403` on failure. *(logAnchor: `Authenticating request: POST`)* |
| 10 | failure-point  | Score handler entry                                    | FabricDS-ModelEndpoint · `json_endpoint.score_handler` · L91                                                                    | NGINX `proxy_pass` to the per-version model-runner; Gunicorn dispatches POST to the Flask `score` blueprint. Body parsed as JSON and Pydantic-validated. `400 InvalidInput` on parse/validation error. *(logAnchor: `Hit [JSON] Probe`)* |
| 11 | failure-point  | Load model (cold-start path)                           | FabricDS-ModelEndpoint · `json_endpoint.get_cached_predict_utils` · L55                                                         | Returns cached MLflow `PredictUtils` or loads fresh under a double-checked lock. On error raises `RuntimeError`; `score_handler` returns `500`. *(logAnchor: `Model loaded and cached successfully`)* |
| 12 | failure-point  | Format branch + tensor-flight gate                     | FabricDS-ModelEndpoint · `json_endpoint.py` · L182                                                                              | Branches on `formatType`. Tensor branch is gated by `is_tensor_support_enabled()` (reads `MODELENDPOINT_TENSOR_SUPPORT` env var set by the workload's tensor-support flight). Returns `400 UnsupportedItemType` if flight is OFF. *(logAnchor: `Loading inputs as`)* |
| 13 | failure-point  | MLflow predict()                                       | FabricDS-ModelEndpoint · `json_endpoint.translate_inputs_outputs_and_predict` · L202                                            | Invokes the model's `predict()` — TensorFlow / PyTorch / scikit-learn / XGBoost. Framework exceptions map to `400`; anything else becomes `500 GenericExceptionError`. *(logAnchor: `Returning model prediction output back to caller`)* |
| 14 | call-step      | Emit certified `InvokeModelEndpointVersion` event      | workload-ml · `ModelEndpointManagerACA` · L1095                                                                                 | Always fires regardless of HTTP outcome. `ActivityStatus = Succeeded` on 200, else `Failed`. The durable audit record that a score attempt happened. |
| 15 | call-step      | Fire-and-forget update of last-score-time              | workload-ml · `ModelEndpointManagerACA` · L1099                                                                                 | Only on `IsSuccessStatusCode`. `Task.Run` updates Cosmos `lastScoreTime`. Does not block the response; failures are tolerated. Consumed downstream by auto-sleep / scale-down decisions. |
| 16 | call-step      | Emit `Score` / `Succeeded` model-monitoring event      | workload-ml · `ModelEndpointManagerACA` · L1100                                                                                 | Model-monitoring telemetry with `ActivityStatus.Succeeded`, operation name, response status, version, default-version flag, input row count, model flavor. Drives operational dashboards. Failure branch emits the `Failed` variant. |
| 17 | call-step      | Return scored response to caller                       | workload-ml · `ModelEndpointManagerACA` · L1110                                                                                 | Returns `HttpResponseMessage` carrying the ACA container's prediction payload back through `ScoreVersionAsync` → controller → client. Synchronous request ends here. |
